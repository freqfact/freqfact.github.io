<footer class="bg-transparent py-8">
    <div class="container mx-auto px-4">
        <div class="flex flex-wrap justify-between items-center">
            <div class="w-full md:w-auto mb-4 md:mb-0">
                <p class="text-glow uppercase">&copy; 2024 FREQFACT. All rights reserved.</p>
            </div>
            <div class="w-full md:w-auto">
                <nav class="flex flex-wrap gap-6">
                    <a href="/events" class={`uppercase text-glow hover:text-yellow-400 transition-all ${Astro.url.pathname === "/events" ? "text-yellow-400 font-bold" : "text-white"}`}>Events</a>
                    <a href="/about" class={`uppercase text-glow hover:text-yellow-400 transition-all ${Astro.url.pathname === "/about" ? "text-yellow-400 font-bold" : "text-white"}`}>About</a>
                    <a href="/contact" class={`uppercase text-glow hover:text-yellow-400 transition-all ${Astro.url.pathname === "/contact" ? "text-yellow-400 font-bold" : "text-white"}`}>Contact</a>
                    <a href="/links" class={`uppercase text-glow hover:text-yellow-400 transition-all ${Astro.url.pathname === "/links" ? "text-yellow-400 font-bold" : "text-white"}`}>Links</a>
                </nav>
            </div>
        </div>
    </div>
</footer>

<style>
    .text-glow {
        color: white;
        text-shadow: 0 0 50px rgba(255, 255, 255, 0.5);
    }
    
    footer {
        position: fixed;
        bottom: 0;
        left: 0;
        right: 0;
        width: 100%;
    }
</style>
